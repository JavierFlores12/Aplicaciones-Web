//Notaciones 
//; --> Deliminar el final de una linea (NO SIEMPRE SE REQUIERE)
const a = 1;
a;  
//. --> accede a los atributos de un objeto
//(Ejemplo) alumno.edad;

//[] --> Square brackets Listas, arrays
//Declarar
const ar = [1, 2, 3, 4];
//Acceder al Array
ar[2]
console.log(ar[2])

//{} --> Curly brackets 
// Creación de objetos, establecer funciones o para agragar estructuras.
//(Ejemplo) 
const alumno = {
    edad: 15,
    estatura: 1.70
}
if(a > b){
    //Estrucutra de control
}
for(const x = 1, 1 = i, i++){
    //Estructura de control
}

//() --> Parenthesis funciones. Sirven para asignar valores a un función.
function promedio(a, b, c){
    //Todo el proceso de la función
}
if(a > b){
    //Procesos
}